# TEMPLATE — copie para .env.local antes de rodar o projeto
# NUNCA versione arquivos com valores reais
#
# Setup inicial (rode uma vez após clonar):
#   cp envs/.env.example envs/.env.local
#
# Subir local:  npm run dev

# API do backend
VITE_API_BASE_URL=http://localhost:8080
